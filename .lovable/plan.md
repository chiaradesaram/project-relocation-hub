Update the shared app background color token to `#6459a1` and leave every other color, layout, component, and interaction unchanged.

Technical detail:
- Change the existing background token in `src/styles.css` to the OKLCH equivalent of `#6459a1`: `oklch(0.5078 0.1119 288.78)`.
- Do not modify cards, nav, pills, portfolio rows, action buttons, spacing, or page structure.
- Verify the preview still renders with the new background.